---
"mcp-tada": minor
---

Initial release: `initMcpTada` types MCP tool calls from an introspected `tools/list` snapshot, and the `mcp-tada introspect` and `mcp-tada check` commands generate and verify that snapshot.
