---
"mcp-tada": minor
---

The type mapper handles `patternProperties` (one index signature over the union of the pattern schemas) and `if`/`then`/`else` (the union of the base schema with each branch applied).
