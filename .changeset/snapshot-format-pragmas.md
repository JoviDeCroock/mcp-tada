---
"mcp-tada": patch
---

Generated snapshots now start with `/* eslint-disable */` and carry a `/* prettier-ignore */` line, so a project formatter no longer unquotes the keys and breaks `mcp-tada check`.
